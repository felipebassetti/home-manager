{
  "name": "flatsharing-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --local --port 8787",
    "dev:local": "wrangler dev --local --port 8787",
    "dev:cloudflare": "wrangler dev --remote --port 8787",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260405.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.13.2"
  }
}
